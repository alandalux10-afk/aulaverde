node_modules/
actualizar.js
instalar.js
*.bak

# Datos de runtime y generados por la app: NUNCA deben versionarse
# (contienen la base de datos con credenciales, datos personales de clientes
# sujetos a RGPD, PDFs de facturas, tickets y backups)
data/
descargas/
*.db
*.db-journal

# Variables de entorno / secretos
.env
.env.local

# Clave privada de licencias: NUNCA debe subirse a git. Con ella se pueden
# fabricar licencias válidas para el producto — es la pieza más sensible
# de todo el proyecto. Solo debe existir en el ordenador de Mario.
herramientas/clave-privada.pem

# Sistema
appPOS80AMUSE
npm
dist/ 
